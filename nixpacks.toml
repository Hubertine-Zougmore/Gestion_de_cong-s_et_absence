[phases.setup]
nixPkgs = ['php82', 'php82Extensions.ldap', 'php82Extensions.gd', ...]
[phases.install]
cmds = ['composer install --optimize-autoloader --no-dev']

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=${PORT:-8000}'
